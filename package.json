{
  "name": "verona-interfaces_specification_schemer",
  "license": "CC0-1.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/verona-interfaces/schemer"
  },
  "scripts": {
    "generate": "asyncapi generate fromTemplate ./schemerapi.yaml @asyncapi/html-template -o ./docs --force-write --use-new-generator -p singleFile=true"    
  },
  "devDependencies": {
    "@asyncapi/cli": "^2.11.0",
    "@asyncapi/html-template": "^3.1.0"
  }
}
